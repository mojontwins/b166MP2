# Important! Updates needed in the other projects.

- `server/ConsoleCommandHandler.java` - Fixed command line parser when writing in the server so it doesn't call the user-based custom command line system 